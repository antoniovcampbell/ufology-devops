apiVersion: v1
kind: Service
metadata:
  name: ufology-app-service
  namespace: ufology
  labels:
    app: ufology-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ufology-app