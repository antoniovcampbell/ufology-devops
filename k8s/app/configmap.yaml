apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ufology
  labels:
    app: ufology-app
data:
  DB_NAME: "ufology"
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  SPRING_PROFILES_ACTIVE: "k8s"